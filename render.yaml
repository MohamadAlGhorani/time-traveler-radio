services:
  - type: web
    name: time-traveler-radio
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
